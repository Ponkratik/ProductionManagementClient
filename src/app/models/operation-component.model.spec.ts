import { OperationComponent } from './operation-component.model';

describe('OperationComponent', () => {
  it('should create an instance', () => {
    expect(new OperationComponent()).toBeTruthy();
  });
});
